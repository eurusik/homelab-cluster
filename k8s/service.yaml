apiVersion: v1
kind: Service
metadata:
  name: homelab-docs
  namespace: homelab-docs
  labels:
    app: homelab-docs
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: homelab-docs
